# Gauss–Manin / Hodge-norm integration — status

## Goal
Integrate KZ along g_t generated by φ₊ so the Prym complement expands; recover sum23 ≈ 3/5.

## Probes run

| Metric / method | pos_sum | sum23 | notes |
|-----------------|---------|-------|-------|
| Eigenvalues of φ₊ / log ρ | 1.0 | 0.0 | only ±1 |
| Edge Gram G-singular values | ~2.00 | **~0.34** | intermediate, not 0.6 |
| Period-flat norm growth Mⁿ | 1.0 | 0.0 | only tautological |
| Random Veech walk (cylinder twists) | ~1.09 | ~0.09 | same regime as before |

## Structural issue in current frame

Period matrix of the 6 basis chains has **rank 2**:
- all H-cores have periods (width, 0) — parallel
- all V-cores have periods (0, height) — parallel

Non-tautological expansion cannot appear in period norm on this basis.

## Conclusion

φ₊ is the correct unit (ρ = 2+√3). Cylinder-twist monodromy is symplectic.
**Neither eigenvalue nor Hodge/period norm of this φ₊ in the current origami H₁ frame recovers sum23 = 3/5.**

The complement is effectively frozen in period geometry (rank-2 periods).

## Next (still this gate)

Rebuild the 6D H₁ / period lattice from the **polygonal S(1,−2)** so that absolute periods span the full 6-dimensional space (not just pure H/V cylinder cores). Then re-evaluate φ₊ and Gauss–Manin along g_t.

`promote_ready: false`
