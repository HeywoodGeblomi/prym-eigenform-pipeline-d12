# Gate 1 attempt — 2026-08-04

## Result: **FAIL** (Gate 1 remains OPEN)

### Attempt 1 — Transcribe arXiv:2210.13503 Fig. 4.3

- Figure obtained (PDF page 15, 200 DPI render).
- **Visible labels:** heights `1`, `λ`, `1`; widths `w−λ`, `λ`, `w−λ`.
- **No discrete edge alphabet** (no A,B,C letter labels).
- For `(w,e)=(1,±2)`: `w−λ = −√3 < 0`, so the figure’s length scheme does not apply directly to the classical D=12 parameters (it assumes `λ < w`).

### Attempt 2 — sage-flatsurf

- `flatsurf` / `surface_dynamics` **not installed** in this environment.

### Residual-0

- Prototype period vector still has residual **0** (unchanged algebraic asset).

### Decision

Do **not** mark Gate 1 PASS. Do **not** promote hypothesis permutation to certified.
Phase 2 geometric Track B remains blocked.

### Next paths to close Gate 1

1. Labelled figure with edge letters for `S(1,±2)`.
2. Install sage-flatsurf and construct the surface.
3. Author-supplied return map / generalized permutation.
